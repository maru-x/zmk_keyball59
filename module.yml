name: click_back

build:
  cmake: |
    target_sources(app PRIVATE
      ${CMAKE_CURRENT_SOURCE_DIR}/app/src/behaviors/behavior_mouse_click_back.c
    )
  kconfig:
    load: app
    fragment: |
      config ZMK_BEHAVIOR_MOUSE_CLICK_BACK
          bool "Mouse Click with Auto-Return"
          default y

settings:
  # app/dts/bindings 以下を検索
  dts_root: app/dts
